name: Build and Publish CANN Test Image

on:
  workflow_call:
    inputs:
      cann_tag:
        description: 'CANN unique tag'
        required: true
        type: string
      cann_version:
        description: 'CANN version'
        required: true
        type: string
      py_version:
        description: 'Python version'
        required: true
        type: string
      os_version:
        description: 'OS version'
        required: true
        type: string
      soc:
        description: 'SOC version (CANN package naming)'
        required: true
        type: string
      dockerfile:
        description: 'Test Dockerfile filename'
        required: true
        type: string
      push:
        description: 'Push images to registries'
        required: false
        type: boolean
        default: true
    secrets:
      HW_USERNAME_DAILY:
        description: 'Huawei Cloud SWR username for daily'
        required: true
      HW_TOKEN_DAILY:
        description: 'Huawei Cloud SWR token for daily'
        required: true
      QUAY_DAILY_USERNAME:
        description: 'Quay username for daily'
        required: true
      QUAY_DAILY_TOKEN:
        description: 'Quay token for daily'
        required: true
  workflow_dispatch:
    inputs:
      cann_tag:
        description: 'CANN unique tag'
        required: true
        type: string
        default: '2026.07.29-a3-ubuntu22.04-py3.12'
      cann_version:
        description: 'CANN version'
        required: true
        type: string
        default: '9.2.0-20260918144839'
      py_version:
        description: 'Python version'
        required: true
        type: string
        default: '3.12.13'
      os_version:
        description: 'OS version'
        required: true
        type: string
        default: '22.04'
      soc:
        description: 'SOC version (CANN package naming)'
        required: true
        type: string
        default: 'A3'
      dockerfile:
        description: 'Test Dockerfile filename'
        required: true
        type: string
        default: 'Dockerfile.ubuntu.cann.test'
      push:
        description: 'Push images to registries'
        required: false
        type: boolean
        default: true

jobs:
  build-push-digest:
    name: build ${{ inputs.cann_tag }} (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            platform: linux/amd64
            runner: ubuntu-latest
          - arch: arm64
            platform: linux/arm64
            runner: ubuntu-22.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Free up disk space
        uses: jlumbroso/free-disk-space@54081f138730dfa15788a46383842cd2f914a1be # v1.3.1
        with:
          tool-cache: true
          docker-images: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Quay
        if: ${{ inputs.push }}
        uses: docker/login-action@v4
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_DAILY_USERNAME }}
          password: ${{ secrets.QUAY_DAILY_TOKEN }}
          ecr: false
          logout: false

      - name: Login to Huawei Cloud SWR
        if: ${{ inputs.push }}
        env:
          HW_USERNAME_DAILY: ${{ secrets.HW_USERNAME_DAILY }}
          HW_TOKEN_DAILY: ${{ secrets.HW_TOKEN_DAILY }}
        run: |
          docker login -u cn-north-4@"$HW_USERNAME_DAILY" -p "$HW_TOKEN_DAILY" swr.cn-north-4.myhuaweicloud.com

      - name: Compute CANN short version
        id: cann
        env:
          CANN_VERSION: ${{ inputs.cann_version }}
        run: |
          SHORT_VERSION=$(echo "${CANN_VERSION}" | grep -oE '[0-9]+(\.[0-9]+)+' | head -1 | cut -d'.' -f1-3)
          echo "short_version=${SHORT_VERSION}" >> $GITHUB_OUTPUT

      - name: Build (PR check, no push)
        if: ${{ !inputs.push }}
        uses: docker/build-push-action@v7
        with:
          platforms: ${{ matrix.platform }}
          file: ${{ github.workspace }}/cann_test_version/${{ inputs.dockerfile }}
          context: ${{ github.workspace }}
          build-args: |
            OS_VERSION=${{ inputs.os_version }}
            PY_VERSION=${{ inputs.py_version }}
            CANN_VERSION=${{ inputs.cann_version }}
            SOC_VERSION=${{ inputs.soc }}
            CANN_SHORT_VERSION=${{ steps.cann.outputs.short_version }}
          push: false
          provenance: false

      - name: Build and push by digest
        if: ${{ inputs.push }}
        uses: docker/build-push-action@v7
        id: build
        with:
          platforms: ${{ matrix.platform }}
          tags: quay.io/atlas_inference/cann
          file: ${{ github.workspace }}/cann_test_version/${{ inputs.dockerfile }}
          context: ${{ github.workspace }}
          build-args: |
            OS_VERSION=${{ inputs.os_version }}
            PY_VERSION=${{ inputs.py_version }}
            CANN_VERSION=${{ inputs.cann_version }}
            SOC_VERSION=${{ inputs.soc }}
            CANN_SHORT_VERSION=${{ steps.cann.outputs.short_version }}
          outputs: type=image,push-by-digest=true,name-canonical=true,push=true
          provenance: false

      - name: Install skopeo
        if: ${{ inputs.push }}
        run: |
          sudo apt update
          sudo apt install -y skopeo

      - name: Login to Huawei Cloud SWR (skopeo)
        if: ${{ inputs.push }}
        env:
          HW_USERNAME_DAILY: ${{ secrets.HW_USERNAME_DAILY }}
          HW_TOKEN_DAILY: ${{ secrets.HW_TOKEN_DAILY }}
        run: |
          echo "$HW_TOKEN_DAILY" | skopeo login \
            --username "cn-north-4@$HW_USERNAME_DAILY" \
            --password-stdin \
            swr.cn-north-4.myhuaweicloud.com

      - name: Copy to SWR temp
        if: ${{ inputs.push }}
        run: |
          for i in 1 2 3; do
            echo "=== Attempt $i/3 ==="
            skopeo copy --format v2s2 \
              docker://quay.io/atlas_inference/cann@${{ steps.build.outputs.digest }} \
              docker://swr.cn-north-4.myhuaweicloud.com/inference/cann_temp:${{ inputs.cann_tag }}-${{ matrix.arch }} && exit 0
            echo "Attempt $i failed, retrying in $((i*30))s..."
            sleep $((i*30))
          done
          echo "All 3 attempts failed"
          exit 1

      - name: Export digest
        if: ${{ inputs.push }}
        run: |
          mkdir -p ${{ runner.temp }}/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "${{ runner.temp }}/digests/${digest#sha256:}"

      - name: Upload digest
        if: ${{ inputs.push }}
        uses: actions/upload-artifact@v4
        with:
          name: digests-${{ inputs.cann_tag }}-${{ matrix.arch }}
          path: ${{ runner.temp }}/digests/*
          if-no-files-found: error
          retention-days: 1

  push-image:
    name: manifest ${{ inputs.cann_tag }}
    runs-on: ubuntu-latest
    needs: build-push-digest
    if: ${{ inputs.push }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download digests
        uses: actions/download-artifact@v4
        with:
          path: ${{ runner.temp }}/digests
          pattern: digests-${{ inputs.cann_tag }}-*
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Quay
        uses: docker/login-action@v4
        with:
          registry: quay.io
          username: ${{ secrets.QUAY_DAILY_USERNAME }}
          password: ${{ secrets.QUAY_DAILY_TOKEN }}

      - name: Create manifest list and push
        working-directory: ${{ runner.temp }}/digests
        run: |
          docker buildx imagetools create -t quay.io/atlas_inference/cann:${{ inputs.cann_tag }} \
            $(printf 'quay.io/atlas_inference/cann@sha256:%s ' *)

      - name: Login to Huawei Cloud SWR
        env:
          HW_USERNAME_DAILY: ${{ secrets.HW_USERNAME_DAILY }}
          HW_TOKEN_DAILY: ${{ secrets.HW_TOKEN_DAILY }}
        run: |
          docker login -u cn-north-4@"$HW_USERNAME_DAILY" -p "$HW_TOKEN_DAILY" swr.cn-north-4.myhuaweicloud.com

      - name: Create SWR manifest list and push
        run: |
          docker buildx imagetools create -t swr.cn-north-4.myhuaweicloud.com/inference/cann:${{ inputs.cann_tag }} \
            swr.cn-north-4.myhuaweicloud.com/inference/cann_temp:${{ inputs.cann_tag }}-amd64 \
            swr.cn-north-4.myhuaweicloud.com/inference/cann_temp:${{ inputs.cann_tag }}-arm64
